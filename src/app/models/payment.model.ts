export interface Payment {
    // name: string;
    // url: string;
    creditCardNumber: string;
    cardHolderName: string;
    expirationDate: string;
    securityCode: string;
    amount: string;
}